# (内容は変更なし、PROJECT_NUMBERは上で取得した変数から自動入力)
# functions/.env.yaml
# Secret Manager から OAuth クライアント ID を読み込む
# この Functions を呼び出す Streamlit アプリケーションの OAuth クライアント ID を指定します。
GOOGLE_OAUTH_CLIENT_ID: projects//secrets/google-client-id/versions/latest
